<input type="submit" value="@ActionService.SaveName" class="btn btn-primary" />
@Html.ActionLink(ActionService.CancelName, ActionService.Cancel, ActionService.Controller, new { area = ActionService.Area }, new { @class = "btn btn-danger" })
